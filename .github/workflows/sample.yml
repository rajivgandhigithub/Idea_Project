name: Share data between steps

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1 - set variable
        run: echo "MY_VAR=Hello Rajiv" >> $GITHUB_ENV

      - name: Step 2 - use variable
        run: echo "Value is $MY_VAR"

           
          
